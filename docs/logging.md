# Logging Conventions

**Moved.** Operator audit logging for `drot` is documented in the DROT submodule:

→ [`tooling/drot/docs/logging.md`](../tooling/drot/docs/logging.md)

Cursor rule for log changes: [`tooling/drot/.cursor/rules/logging.mdc`](../tooling/drot/.cursor/rules/logging.mdc).

Storage-specific product docs remain under this `docs/` tree (DSFD, ABI, native packaging, storage CI).
